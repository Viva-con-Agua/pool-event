apiVersion: v1
kind: Service
metadata:
  name: pool-event
  labels: &labels
    app.kubernetes.io/name: pool-event
    app.kubernetes.io/component: component-explorer
spec:
  selector: *labels
  ports:
    - name: http
      port: 80
      targetPort: 1323
