apiVersion: v1
kind: Service
metadata:
  name: pool-event-db
  labels: &labels
    app.kubernetes.io/name: pool-event-db
    app.kubernetes.io/component: component-explorer
spec:
  selector: *labels
  ports:
    - name: mongo
      protocol: TCP
      port: 27017
      targetPort: 27017
